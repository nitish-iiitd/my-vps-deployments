apiVersion: v1
kind: Service
metadata:
  name: {{ include "triply.fullname" . }}-frontend
  labels:
    app.kubernetes.io/name: {{ include "triply.name" . }}-frontend
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "triply.name" . }}-frontend
    app.kubernetes.io/instance: {{ .Release.Name }}
