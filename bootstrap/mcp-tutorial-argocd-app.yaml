apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-tutorial
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/nitish-iiitd/my-vps-deployments.git
    targetRevision: main
    path: apps/mcp-tutorial
    helm:
      releaseName: mcp-tutorial

  destination:
    server: https://kubernetes.default.svc
    namespace: apps

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true